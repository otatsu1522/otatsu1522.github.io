---
interface Props {
  ja: string;
  en: string;
  class?: string;
}

const { ja, en, class: className = '' } = Astro.props;
---

<span class:list={['js-ja', className]}>{ja}</span><span
  class:list={['js-en', 'hidden', className]}>{en}</span
>
